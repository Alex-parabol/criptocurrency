import React from 'react'
import styled from '@emotion/styled'

const Boton = styled.input`
    margin-top: 20px;
    font-weight: bold;
`

const Form =()=>{
    return (
        <form >
            
        </form>
    );
}

export default Form;